---
import BaseLayout from "../../layouts/BaseLayout.astro";
import { BlogHero } from "../../islands/Blogs/BlogHero";

const { slug } = Astro.params;
export const prerender = false;

---

<BaseLayout>
  <BlogHero client:load slug={slug} />
</BaseLayout>
